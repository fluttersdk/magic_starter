// DropdownMenu component — folder-local barrel.
//
// Canonical Wave 4 atomic-component shape. The preview is NOT re-exported;
// `previews:refresh` discovers `*.preview.dart` files directly.

export 'dropdown_menu.dart' show MSDropdownMenu, MSDropdownMenuItem;
export 'dropdown_menu.recipe.dart';
